// Frozen mini-catalog: the real effect, imported so a change to the contract shows up in
// the golden HTML, while fonts and palettes stay fixture data that content PRs cannot move.
export { default } from '../../../../effects/plain.js'
